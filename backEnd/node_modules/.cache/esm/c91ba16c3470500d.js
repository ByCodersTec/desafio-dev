let moment;_347‍.x([["SET_DATE_FORMAT",()=>SET_DATE_FORMAT],["GET_DATE_FORMAT",()=>GET_DATE_FORMAT]]);_347‍.w("moment",[["default",["moment"],function(v){moment=v}]]);

       const SET_DATE_FORMAT = function (val)
{
  return moment(val || new Date())
    .utcOffset(process.env.TIMEZONE)
    .format('YYYY-MM-DDTHH:mm:ss.SSSZ');
}

       const GET_DATE_FORMAT = function (model, col)
{
  return moment(model.getDataValue(col))
    .utcOffset(process.env.TIMEZONE)
    .format('YYYY-MM-DDTHH:mm:ss.SSSZ');
}
