setup:
  addons:
  - plan: heroku-postgresql:hobby-dev
build:
  docker:
    server: backend/Dockerfile
release:
  command:
    - npm run migrate:deploy && npm run prisma:seed
  image: server
run:
  server: bash -c "npm run start"
