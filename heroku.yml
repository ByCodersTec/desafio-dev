setup:
  addons:
  - plan: heroku-postgresql:hobby-dev
build:
  docker:
    web: backend/Dockerfile
release:
  command:
    - npm run migrate:deploy && npm run prisma:seed
  image: web
run:
  web: bash -c "npm run start"
